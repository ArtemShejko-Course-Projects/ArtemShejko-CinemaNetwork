<Window x:Class="Cinema_CP_WPF.Views.AdminViews.CityView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cinema_CP_WPF.Views.AdminViews"
        mc:Ignorable="d"
        Title="CityView" Height="450" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.10*"/>
        </Grid.RowDefinitions>
        <GroupBox Header="City List" Grid.Row="0">
            <ListBox Background="MintCream" ItemsSource="{Binding SortedCityList}" SelectedItem="{Binding SelectedCity}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CityTitle}"></TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
        <GroupBox Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Grid.ColumnSpan="2" Background="Honeydew" Text="{Binding SelectedCity.CityTitle}" Margin="3,3,3,3"/>
                <Button Grid.Column="2" Grid.Row="1" Background="LightSteelBlue" Content="Add City" Margin="5,5,5,5" Command="{Binding AddCity}"></Button>
                <Button Grid.Column="3" Grid.Row="2" Background="LightSteelBlue" Content="Delete City" Margin="5,5,5,5" Command="{Binding DeleteCity}"></Button>
                <Button Grid.Column="4" Grid.Row="3" Background="LightSteelBlue" Content="Save" Margin="5,5,5,5" Command="{Binding SaveChanges}"></Button>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
